import { StyleSheet } from "react-native";

export default StyleSheet.create({
  titleText: {
    fontSize: 32,
    fontWeight: "700",
    color: "#343434",
  },
  hoursText: {
    fontSize: 14,
  },
});
